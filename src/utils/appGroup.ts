/** Must match `app.json`'s entitlements, the widget target config, and
 * `expo-share-intent` — a mismatch is silently empty. Import-free on purpose. */
export const APP_GROUP = "group.com.dexterplanner";
